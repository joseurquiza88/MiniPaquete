# what_time() works

    Code
      what_time(language = "bla")
    Condition
      Error in `what_time()`:
      ! `language` must be one of "es" or "en", not "bla".

# bla() works

    Code
      print(pi)
    Output
      [1] 3.14

